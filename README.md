# Cox PH Risk Score – Streamlit App

A Streamlit app to serve a trained Keras Cox-PH model with a scikit-learn preprocessor.

## Run locally
```bash
pip install -r requirements.txt
streamlit run app.py
